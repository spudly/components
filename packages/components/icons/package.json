{
  "name": "@spudly/icons",
  "main": "build/index.js",
  "mainSrc": "src/index.ts",
  "version": "0.1.0",
  "scripts": {
    "build": "rm -rf build && tsc -p tsconfig.build.json",
    "prepare": "npm run build",
    "test": "jest"
  },
  "license": "ISC",
  "repository": "https://github.com/spudly/components/tree/masterpackages/components/icons",
  "files": [
    "build"
  ],
  "sideEffects": false,
  "dependencies": {
    "react": "^16.10.2"
  }
}
